To start the Go API server (http://localhost:8080/api/fibonacci/), inside the "backend" directory run:

    $ go run .

To start the Svelte UI server (http://localhost:5173), inside the "frontend" directory run:

    $ npm install

    $ npm start
